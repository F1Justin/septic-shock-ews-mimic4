# ── PhysioNet downloaded files (index pages, waveform records) ──
physionet.org/

# ── MIMIC-IV raw data (PhysioNet DUA — do not redistribute) ──
mimiciv/

# ── eICU raw data (PhysioNet DUA — do not redistribute) ──
eicu-crd/

# ── Derived patient-level data (still under DUA) ──
data/

# ── Third-party reference materials & internal documents ──
icu-csd-ref/
mimic-code-ref/
重要文献/
审查报告/

# ── AC1 sub-project: local-only or generated files ──
ac1/submission/
ac1/logs/
ac1/output/old/
ac1/sn-article-template/
ac1/report.pdf
ac1/report_zh.*
ac1/manuscript_*.zip
meeting/

# ── eICU validation sub-project: generated outputs (not for main repo) ──
eicu_validation/output/

# ── Euler EWS sub-project: generated outputs and compiled PDFs (not for main repo) ──
euler_ews/output/
euler_ews/manuscript/*.pdf
euler_ews/manuscript/*.aux
euler_ews/manuscript/*.log
euler_ews/manuscript/*.out

# ── LaTeX build artifacts (project-wide) ──
*.aux
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
*.bbl
*.blg
*.nav
*.snm
*.vrb
*.run.xml

# ── Merged manuscript: track tex/bib/figures; ignore build artifacts ──
# (build artifacts already caught by global *.aux, *.log etc. patterns below)
merged_manuscript/*.pdf

# ── Other regenerated or draft files ──
cover_letter.pdf
strobe_checklist.pdf
meeting_slides.pdf
zh-report.pdf
*_副本.pdf
report_副本.tex

# ── Python ──
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# ── Runtime logs ──
logs/

# ── Local comparison / scratch directories ──
remote_rerun_compare/

# ── OS / editor ──
.DS_Store
.idea/
.vscode/
.cursor/

# Note: references.bib is tracked (required for report.tex on GitHub).
