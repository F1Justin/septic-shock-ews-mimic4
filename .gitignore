# ── MIMIC-IV raw data (PhysioNet DUA — do not redistribute) ──
mimiciv/

# ── Derived patient-level data (still under DUA) ──
data/

# ── Third-party reference materials & internal documents ──
icu-csd-ref/
mimic-code-ref/
重要文献/
审查报告/

# ── Old / experimental results ──
output/old/

# ── LaTeX build artifacts ──
*.aux
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
*.bbl
*.blg
*.bib

# ── Journal submission template (local reference only) ──
sn-article-template/

# ── Chinese manuscript version ──
report_zh.*
zh-report.*

# ── Python ──
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# ── Runtime logs ──
logs/

# ── Local comparison / scratch directories ──
remote_rerun_compare/

# ── OS / editor ──
.DS_Store
.idea/
.vscode/
.cursor/
