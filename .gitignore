# ── MIMIC-IV raw data (PhysioNet DUA — do not redistribute) ──
mimiciv/

# ── Derived patient-level data (still under DUA) ──
data/

# ── Third-party reference materials & internal documents ──
icu-csd-ref/
mimic-code-ref/
重要文献/
审查报告/

# ── Local-only: cover letter, STROBE checklist, meeting slides (keep on disk, not on GitHub) ──
submission/
meeting/

# ── Old / experimental results ──
output/old/

# ── LaTeX build artifacts ──
*.aux
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
*.bbl
*.blg
*.nav
*.snm
*.vrb
*.run.xml

# ── Regenerated PDFs (compile from .tex; keeps diffs small on GitHub) ──
report.pdf
cover_letter.pdf
strobe_checklist.pdf
meeting_slides.pdf
zh-report.pdf
*_副本.pdf

# ── Draft / duplicate manuscript (keep locally if needed) ──
report_副本.tex

# ── Journal submission template (large; download Springer Nature template separately) ──
sn-article-template/

# ── Chinese manuscript version (optional local copy) ──
report_zh.*
zh-report.*

# ── Python ──
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# ── Runtime logs ──
logs/

# ── Local comparison / scratch directories ──
remote_rerun_compare/

# ── OS / editor ──
.DS_Store
.idea/
.vscode/
.cursor/

# Note: references.bib is tracked (required for report.tex on GitHub).
